POSTGRES_DB=asset_tracker
POSTGRES_USER=asset_tracker
POSTGRES_PASSWORD=change-me-for-local-only
POSTGRES_PORT=5432

DATABASE_URL=postgresql://asset_tracker:change-me-for-local-only@localhost:5432/asset_tracker
NEXT_PUBLIC_APP_ENV=local
APP_PORT=3000

# HR inbound webhook signing secret (HMAC-SHA256)
HR_WEBHOOK_SECRET=replace-with-a-long-random-secret
